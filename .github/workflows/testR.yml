name: 'Weekday chart'

on:
# This command allows us to run the Action manually from the Actions tab.
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    name: hi
    steps:
     - name: generate chart
       uses: brandmaier/weekdaychart@238b1e15f164dc7f9fab7912c13124d7d0d0cf61
